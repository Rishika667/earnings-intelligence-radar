"""Small shared utilities with no business logic of their own (e.g. date
helpers, logging setup). Kept minimal deliberately — utility sprawl is a
common source of unnecessary complexity.

Status: Stage 1 — placeholder only.
"""
